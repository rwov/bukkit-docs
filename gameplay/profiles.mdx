---
title: "Player profiles and skulls"
description: "Work with PlayerProfile, textures, identity, and caching."
icon: "id-card"
---

# Player profiles and skulls

`PlayerProfile` represents identity plus optional properties such as textures. A profile can be incomplete; completing or resolving it may involve external profile services.

## Create and inspect profiles

- Use the server API to create profiles by UUID and/or name.
- Check whether the profile has the properties your output requires.
- Do not treat a name-only profile as a verified durable identity.
- Copy or cache stable texture results with an expiry rather than resolving on every render.

## Apply to skulls

- Create a player-head `ItemStack`, obtain `SkullMeta`, set the owning profile, then write the meta back.
- Perform any blocking profile resolution away from the main thread.
- Before applying an async result, verify the inventory, slot, and session still refer to the same request.
- Respect rate limits and provide a default head on failure.

## Example

```java
ItemStack head = new ItemStack(Material.PLAYER_HEAD);
SkullMeta meta = (SkullMeta) head.getItemMeta();
meta.setOwnerProfile(profile);
meta.setDisplayName("Profile: " + profile.getName());
head.setItemMeta(meta);
```

## Production checklist

- Validate identity, world, and feature ownership at the point of mutation.
- Keep main-thread work bounded; move only isolated data work off-thread.
- Handle disconnect, unload, reload, and disable explicitly.
- Read the exact API contract for the server version you target.